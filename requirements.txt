# DeepFake Zoom Detector - Requirements
# Install with: pip install -r requirements.txt

# Web Framework
fastapi>=0.100.0
uvicorn[standard]>=0.23.0

# Data Validation
pydantic>=2.0.0
pydantic-settings>=2.0.0

# Computer Vision
opencv-python>=4.8.0
numpy>=1.24.0

# Deep Learning — PyTorch (for .pth models)
torch>=2.0.0
torchvision>=0.15.0

# Deep Learning — TensorFlow/Keras (for .keras / .h5 models like Xception)
# FIX: was missing entirely — required for xception_deepfake_image_5o.h5
tensorflow>=2.12.0

# HTTP Client (for Recall.ai integration)
httpx>=0.24.0

# WebSocket (for Recall.ai integration)
websockets>=11.0.0

# Machine Learning Utilities
scikit-learn>=1.3.0
tqdm>=4.65.0
